第09课应该是实验室C语言系列的最后一节课了，整个任务所涉及的主要是数据结构中图的一些内容
主要有：图，邻接表，有向无环图（AOV），拓扑排序，寻路算法（DFS）以及判断图中是否有环……

任务1：主要是图的邻接表，用邻接表储存图的信息.没有难点，要用到一些字符串和结构体的知识

任务2：用栈对邻接表进行拓扑排序，输出拓扑序列.但是我没有用到栈，我的思路是每次只从图中找一个入度为0的顶点，然后把这个顶点删掉，并且把它
      指向的顶点的入度都减1.如果某一次没有找到入度为0的顶点，说明该图中有环.因此这也是一种判断图中是否有环的方法

任务3：首先要判断图中是否有环，用任务2提到的方法即可.
      这是就要分成两种情况：
      1.有环
            这时图中尚未被剔除的顶点即为成环顶点，比较简单
      2.无环：
            这时就是AOV网了.接下来关键就是求最短路径，我在查阅了一些资料之后，决定使用和第8课求哈夫曼编码一样的算法（DFS）.
            具体实现过程就是先用 DFS 遍历图中的所有路径并把路径信息存储下来，然后从中找到最短路径并输出.

具体实现过程可看代码
